engine: fingerprint
info:
    name: AeroCMS Detect
    author: pikpikcu
pscan:
    path:
        - /
    expressions:
        - '"content="(.*)AeroCMS(.*)".bmatches(response.body) && response.status == 200'
