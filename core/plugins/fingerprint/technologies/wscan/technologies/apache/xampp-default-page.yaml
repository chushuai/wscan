engine: fingerprint
info:
    name: XAMPP Default Page
    author: dhiyaneshdk
pscan:
    path:
        - /dashboard/
    expressions:
        - response.body.bcontains(b"<title>Welcome to XAMPP</title>") && response.status == 200
