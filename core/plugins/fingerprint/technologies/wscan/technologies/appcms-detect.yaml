engine: fingerprint
info:
    name: AppCms Detect
    author: princechaddha
pscan:
    path:
        - /
    expressions:
        - '"<!-- Powerd by AppCMS (.*) -->".bmatches(response.body) && response.status == 200'
