engine: fingerprint
info:
    name: CVSweb - Detect
    author: lu4nx
pscan:
    path:
        - /
    expressions:
        - '"content=".*CVSweb.*"".bmatches(response.body) && response.status == 200'
