engine: fingerprint
info:
    name: Amazon Cognito Developer Authentication Sample - Detect
    author: pussycat0x
pscan:
    path:
        - /
    expressions:
        - ( contains(body, "Amazon Cognito Developer Authentication Sample") && contains(body, "Congratulations!") && status_code == 200 )
