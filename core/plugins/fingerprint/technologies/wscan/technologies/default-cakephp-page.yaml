engine: fingerprint
info:
    name: CakePHP Default Page
    author: dhiyaneshdk
pscan:
    path:
        - /
    expressions:
        - ( response.body.bcontains(b"Welcome to CakePHP") && response.body.bcontains(b"CakePHP Issues") ) && response.status == 200
