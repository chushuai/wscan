engine: fingerprint
info:
    name: Default Parallels Plesk Panel Page
    author: dhiyaneshdk
pscan:
    path:
        - /
    expressions:
        - response.body.bcontains(b"<title>Default Parallels Plesk Panel Page</title>") && response.status == 200
