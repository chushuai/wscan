engine: fingerprint
info:
    name: DreamBox Detect
    author: pikpikcu
pscan:
    path:
        - /
    expressions:
        - response.status == 200 && response.body.bcontains(b"<title>Dreambox WebControl</title>")
