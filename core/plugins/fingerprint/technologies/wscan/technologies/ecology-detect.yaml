engine: fingerprint
info:
    name: Ecology Detection
    author: ritikchaddha
pscan:
    path:
        - /
    expressions:
        - response.raw_header.bcontains(b"ecology_JSessionid") && response.status == 200
