engine: fingerprint
info:
    name: Erxes Detect
    author: princechaddha
pscan:
    path:
        - /
    expressions:
        - '"<title>erxes(.*)</title>".bmatches(response.body) && response.status == 200'
