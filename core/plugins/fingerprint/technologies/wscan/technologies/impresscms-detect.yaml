engine: fingerprint
info:
    name: ImpressCMS Detect
    author: princechaddha
pscan:
    path:
        - /
    expressions:
        - '"(?i)(ImpressCMS)".bmatches(response.body) && response.status == 200'
