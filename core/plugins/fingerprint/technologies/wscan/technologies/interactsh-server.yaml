engine: fingerprint
info:
    name: Interactsh Server
    author: pdteam
pscan:
    path:
        - /
    expressions:
        - response.body.bcontains(b"Interactsh Server") && response.status == 200
