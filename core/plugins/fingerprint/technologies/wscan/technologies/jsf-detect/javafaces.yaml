engine: fingerprint
info:
    name: javafaces (JavaServer Faces Detection)
    author: brenocss, moritz nentwig
pscan:
    path:
        - /
    expressions:
        - (contains(body, 'javax.faces.resource') || contains(body, 'javax.faces.ViewState'))
