engine: fingerprint
info:
    name: richfaces (JavaServer Faces Detection)
    author: brenocss, moritz nentwig
pscan:
    path:
        - /
    expressions:
        - ( contains(body, 'richfaces') && contains(body, 'javax.faces.resource') || contains(body, 'javax.faces.ViewState') )
