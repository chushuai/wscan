engine: fingerprint
info:
    name: Kubelet Healthz
    author: sharath
pscan:
    path:
        - /healthz
    expressions:
        - response.status == 200 and "text/plain".bmatches(response.raw_header) and response.body.bcontains(b"ok") and response.raw_header.bcontains(b"Kubernetes")
