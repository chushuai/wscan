engine: fingerprint
info:
    name: Lexmark Device Detect
    author: princechaddha
pscan:
    path:
        - /
    expressions:
        - '"<title>Lexmark (.*)</title>".bmatches(response.body) && response.status == 200'
