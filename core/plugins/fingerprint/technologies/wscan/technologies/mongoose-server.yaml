engine: fingerprint
info:
    name: Mongoose Server
    author: lu4nx
pscan:
    path:
        - /
    expressions:
        - 'response.raw_header.bcontains(b"Server: Mongoose") && response.status == 200'
