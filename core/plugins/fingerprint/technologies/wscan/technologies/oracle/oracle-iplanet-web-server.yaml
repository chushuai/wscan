engine: fingerprint
info:
    name: Detect Oracle-iPlanet-Web-Server
    author: pussycat0x
pscan:
    path:
        - /
    expressions:
        - response.body.bcontains(b"Oracle iPlanet Web Server") && response.status == 200
