engine: fingerprint
info:
    name: Puppet Node Manager
    author: pussycat0x
pscan:
    path:
        - /
    expressions:
        - response.body.bcontains(b"<title>Puppet Node Manager</title>") and response.status == 200
