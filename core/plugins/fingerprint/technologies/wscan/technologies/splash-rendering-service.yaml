engine: fingerprint
info:
    name: Splash - Detect
    author: pussycat0x
pscan:
    path:
        - /
    expressions:
        - response.body.bcontains(b"<title>Splash") and response.body.bcontains(b"splash.loadExample") and response.status == 200
