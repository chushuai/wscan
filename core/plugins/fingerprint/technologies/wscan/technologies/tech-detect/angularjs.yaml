engine: fingerprint
info:
    name: angularjs (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - ( "<(?:div|html)[^>]+ng-app=".bmatches(response.body) || "<ng-app".bmatches(response.body) )
