engine: fingerprint
info:
    name: animate.css (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link [^>]+(?:/([\\d.]+)/)?animate\\.(?:min\\.)?css".bmatches(response.body)'
