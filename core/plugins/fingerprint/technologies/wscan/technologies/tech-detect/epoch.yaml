engine: fingerprint
info:
    name: epoch (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link[^>]+?href="[^"]+epoch(?:\\.min)?\\.css".bmatches(response.body)'
