engine: fingerprint
info:
    name: gitweb (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<!-- git web interface version ([\\d.]+)?".bmatches(response.body)'
