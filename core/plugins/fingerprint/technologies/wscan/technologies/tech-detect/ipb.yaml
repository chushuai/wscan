engine: fingerprint
info:
    name: ipb (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link[^>]+ipb_[^>]+\\.css".bmatches(response.body)'
