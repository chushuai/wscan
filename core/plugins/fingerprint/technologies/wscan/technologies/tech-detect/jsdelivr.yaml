engine: fingerprint
info:
    name: jsdelivr (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<[^>]+?//cdn\\.jsdelivr\\.net/".bmatches(response.body)'
