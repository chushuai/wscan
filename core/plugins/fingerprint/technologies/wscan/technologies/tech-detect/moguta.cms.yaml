engine: fingerprint
info:
    name: moguta.cms (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link[^>]+href=["''][^"]+mg-(?:core|plugins|templates)/".bmatches(response.body)'
