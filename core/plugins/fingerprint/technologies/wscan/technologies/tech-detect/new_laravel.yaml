engine: fingerprint
info:
    name: new_laravel (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '( response.raw_header.bcontains(b"Set-Cookie: {{DN}}_session=") && response.raw_header.bcontains(b"Set-Cookie: XSRF-TOKEN=") )'
