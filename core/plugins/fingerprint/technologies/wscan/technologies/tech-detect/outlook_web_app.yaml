engine: fingerprint
info:
    name: outlook-web-app (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link\\s[^>]*href="[^"]*?([\\d.]+)/themes/resources/owafont\\.css".bmatches(response.body)'
