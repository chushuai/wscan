engine: fingerprint
info:
    name: slick (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link [^>]+(?:/([\\d.]+)/)?slick-theme\\.css".bmatches(response.body)'
