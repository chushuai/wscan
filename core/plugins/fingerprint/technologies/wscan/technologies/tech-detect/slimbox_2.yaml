engine: fingerprint
info:
    name: slimbox-2 (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link [^>]*href="[^/]*slimbox2(?:-rtl)?\\.css".bmatches(response.body)'
