engine: fingerprint
info:
    name: sweetalert2 (Wappalyzer Technology Detection)
    author: hakluke
pscan:
    path:
        - /
    expressions:
        - '"<link[^>]+?href="[^"]+sweetalert2(?:\\.min)?\\.css".bmatches(response.body)'
