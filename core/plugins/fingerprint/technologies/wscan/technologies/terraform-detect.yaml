engine: fingerprint
info:
    name: Detect Terraform Provider
    author: geeknik
pscan:
    path:
        - /provider.tf
    expressions:
        - response.body.bcontains(b"access_key") and response.body.bcontains(b"terraform") and response.status == 200
