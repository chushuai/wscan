engine: fingerprint
info:
    name: Instant Indexing for Google Detection
    author: ricardomaia
pscan:
    path:
        - /wp-content/plugins/fast-indexing-api/readme.txt
    expressions:
        - '"(?i)Stable.tag:\\s?([\\w.]+)".bmatches(response.body)'
