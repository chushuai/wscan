engine: fingerprint
info:
    name: outdated_version (User Role Editor Detection)
    author: ricardomaia
pscan:
    path:
        - /wp-content/plugins/user-role-editor/readme.txt
    expressions:
        - compare_versions(internal_detected_version, concat("< ", last_version))
