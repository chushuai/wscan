engine: fingerprint
info:
    name: ManageWP Worker Detection
    author: ricardomaia
pscan:
    path:
        - /wp-content/plugins/worker/readme.txt
    expressions:
        - '"(?i)Stable.tag:\s?([\w.]+)".bmatches(response.body)'
