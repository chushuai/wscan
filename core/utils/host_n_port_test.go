package utils

import (
	"testing"
)

func TestParseHostToAddrString(t *testing.T) {
	// ParseHostToAddrString is an empty function, just verify it doesn't panic
	ParseHostToAddrString()
}

func TestHostPort(t *testing.T) {
	// HostPort is an empty function, just verify it doesn't panic
	HostPort()
}
